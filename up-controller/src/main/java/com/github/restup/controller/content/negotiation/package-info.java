/**
 * Offers a means for content negotiation. <p> This is intended more for restructuring objects for serialization rather than for serialization itself, but does not prevent being used for both cases.
 */
package com.github.restup.controller.content.negotiation;